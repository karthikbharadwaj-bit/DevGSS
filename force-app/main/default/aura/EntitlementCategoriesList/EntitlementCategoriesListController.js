({
    init: function (component, event, helper) {
        helper.setCategories(component);
    },

    dataChange: function (component, event, helper) {
        helper.setCategories(component);
    }
});