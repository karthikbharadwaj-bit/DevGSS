({
    qwHelperLoaded: function(component, event, helper) {
        // component.set('v.display.proserv', helper.isShowProservTab(component));
    },
    // onActive: function(component, event, helper) {
    //     var id = event.getSource().get('v.id');
    //     var tabCCItem = helper.tabClickCount[id];
    //         helper.tabClickCount[id] = tabCCItem ? tabCCItem + 1 : 1;
    //
    //     if(helper.tabClickCount[id] > 1) {
    //         $A.get("e.c:QuotingToolRefreshQuoteEvent").fire();
    //     }
    // },
})